import { SmallPhotoDirective } from './small-photo.directive';

describe('SmallPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
